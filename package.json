{
  "name": "lilachat",
  "private": true,
  "version": "0.1.0",
  "description": "Mensajer\u00eda privada de la familia \u2014 Android (RN) + web + server propio en la Mac mini",
  "workspaces": [
    "shared",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace=shared && npm run build --workspace=server",
    "test": "npm run test --workspace=shared && npm run test --workspace=server",
    "dev": "npm run dev --workspace=server",
    "emit-tokens": "tsx shared/scripts/emit-tokens.ts"
  }
}
